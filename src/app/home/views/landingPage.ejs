<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <h1 class="govuk-heading-xl">DfE Sign-in</h1>
    </div>
</div>

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <p class="govuk-body-l">DfE Sign-in is the new way to access your DfE online services and has now replaced Secure Access.</p>
        
        <h2 class="govuk-heading-m">Before you start</h2>
        
        <% if (requestOrganisationToggle) { %>
            <p class="govuk-body">You'll either need:</p>
        <% } else { %>
            <p class="govuk-body">You'll need:</p>
        <% } %>
        
        <ul class="govuk-list govuk-list--bullet">
            <li><b>An existing DfE Sign-in account.</b> This will be the email address and password you created when you first accessed DfE Sign-in.</li>

            <% if (requestOrganisationToggle) { %>
                <li><b>To create a DfE Sign-in account.</b>  If you haven’t used DfE Sign-in before you’ll need to <a class="govuk-link-bold" href="<%=profileUrl%>/register">create a DfE Sign-in account</a>.</li>
            <% } %>
        </ul>

        <a class="govuk-button govuk-button--start" href="/my-services" role="button">
            Start now
            <svg class="govuk-button__start-icon" xmlns="http://www.w3.org/2000/svg" width="17.5" height="19" viewBox="0 0 33 40" aria-hidden="true" focusable="false">
                <path fill="currentColor" d="M0 0h13l20 20-20 20H0l20-20z" />
            </svg>
        </a>
        
        <details class="govuk-details" data-module="govuk-details">
            <summary class="govuk-details__summary">
                <span class="govuk-details__summary-text">
                    Services accessed using DfE Sign-in
                </span>
            </summary>
            <div class="govuk-details__text">
                <p class="govuk-body">
                    These DfE services are now accessed using your DfE Sign-in account.
                </p>
                <ol class="govuk-list govuk-list--bullet">
                    <%
                    for (let s = 0; s < services.length; s++) {
                        const service = services[s];
                        if (service.isExternalService === true) {
                    %>
                        <li><%=service.name%></li>
                    <% } %>
                    <% } %>
                </ol>
            </div>
        </details>
    </div>
</div>
