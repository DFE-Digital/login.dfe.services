const getById = async () => {
  return Promise.resolve([]);
};

const updateIndex = async () => {
  return Promise.resolve();
};

const createIndex = async () => {
  return Promise.resolve();
};

module.exports = {
  getById,
  updateIndex,
  createIndex,
};
