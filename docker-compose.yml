version: '3.4'

services:
  logindfeservices:
    image: logindfeservices
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 41012:41012
